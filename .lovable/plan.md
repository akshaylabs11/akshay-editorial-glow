# Targeted portfolio refinements

## What will change
- Refine only the hero and final portrait masks into matching, slightly squarish asymmetric editorial shapes while preserving the current portraits, layout, typography, colors, and subtle motion.
- Add the supplied Loyola Academy logo to Department Fest; retain and verify the existing EZFinanz and NCC logos and their restrained hover treatment.
- Keep all project content intact, retain the existing image lightbox and hover behavior, and connect Project 05 to the real group photo once its supplied file is available.
- Verify all three supplied certificate files, CV download links, and the exact LinkedIn URL; keep the credential grid compact and responsive.
- Validate education, number, and toolkit interactions plus desktop/mobile layout, image loading, links, modal behavior, and horizontal overflow in the rendered site.

## Supplied-file constraint
The current uploads contain two near-identical solo portraits, the hero portrait, project images 01–04, certificates, CV, and institution/company logos. No group photograph is currently present, so Project 05 will remain clearly marked rather than substituting or inventing an image. Once the actual group photo is uploaded, it can be connected without changing the project content or layout.

## Technical details
- Use the existing asset pointer files and current Dialog-based image/certificate viewer.
- Adjust the existing CSS mask utilities rather than rebuilding components.
- Add only the missing Loyola asset import/data entry and narrowly scoped interaction refinements.
